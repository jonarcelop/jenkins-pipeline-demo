pipeline {
    agent any

    stages {
        stage('Saludo desde GitHub') {
            steps {
                echo 'Pipeline ejecutado desde GitHub'
                bat 'echo Hola Jenkins + GitHub'
            }
        }
    }
}
